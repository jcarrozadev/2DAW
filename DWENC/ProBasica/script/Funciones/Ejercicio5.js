/**
 * 5. Desarrollar una función que retorne la cantidad de dígitos que tiene una
    variable entera positiva.
 */

window.addEventListener('load', () => {

    let frase = prompt("Introduce numeros:");

    alert("El recuento de numeros es: "+frase.length);

});